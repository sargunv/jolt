[package]
name = "jolt_cli"
version = "0.20260705.0"
description = "Fast, opinionated JVM and Kotlin Multiplatform project tooling"
repository = "https://github.com/sargunv/jolt"
license = "Apache-2.0"
binaries = ["jolt"]
out-dir = "out"
build-command = ["python", "build.py"]
