---
source: crates/jolt_java_fmt/tests/corpus_fixtures.rs
expression: formatted
---
class B37629610 {
  private static ImmutableList<InputOutputPair> toInputOutputPairs(
    Options options
  ) {
    for (
      Iterator<Path> inputIt = options.inputJars.iterator(),
      outputIt = options.outputJars.iterator();
      inputIt.hasNext();
      
    ) {
    }
    for (
      Iterator<Path> inputIt = options.inputJars.iterator(),
      outputIt = options.outputJars.iterator();
      ;
      inputIt.next()
    ) {
    }
    for (
      inputIt = options.inputJars.iterator(), //
      outputIt = options.outputJars.iterator();
      ;
      inputIt.next()
    ) {
    }
  }
}
