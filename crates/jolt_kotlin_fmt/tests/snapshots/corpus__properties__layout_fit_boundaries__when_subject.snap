---
source: crates/jolt_kotlin_fmt/tests/corpus.rs
expression: snapshot
---
formatted:
fun inspectState(stateMachine: StateMachine): Int =
  when (
    val currentState = stateMachine.computeCurrentStateSnapshotForDiagnostics()
  ) {
    else -> currentState.code
  }

diagnostics:
(none)
